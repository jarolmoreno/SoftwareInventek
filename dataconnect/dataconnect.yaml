specVersion: "v1"
serviceId: "plantilla"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "plantilla-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
